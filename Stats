#!/bin/bash
cpu=$(top -bn1 | grep "Cpu(s)" | awk '{print $2}' | cut -d'%' -f1)
ram=$(free | awk '/Mem:/ {printf "%.0f", $3/$2*100}')
gpu=$(nvidia-smi --query-gpu=utilization.gpu --format=csv,noheader,nounits)

echo "CPU: ${cpu}%  RAM: ${ram}%  GPU: ${gpu}%"
